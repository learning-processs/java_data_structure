properties of oops

Abstraction
2. polymorphism
    Poly -> many Morphism -> Way to represent

    Many way to represent single entity and item;
    Any language that doesn't support -> X object orient 

    Type : 

    COMPILE TIME / STATIC POLYMORPHISM
    -> Achieve via method overloading..
    -> method overloading - same name but type, args, return , ordering different
    e.g multiple constructor.

    RUN-TIME / Dynamic polymorphism: 
    -> Achieve via method overriding..



1. Inheritance

    pass the things to the children
    children can use those things

    Base class
    |           (children can access, means child class inherit the properties of parent class)
    child class

    Type : 
    Single Inheritance : One class extends another class
    Multilevel Inheritance : // JAVA not allowed the multiple inheritance -> use Intefaces
    Hierarchial Inheritance : possible many classes extends the parent one class property.
    Hybrid Inheritance : // Not allowed
Encaplustaion